name: ci.yml (mirror-inert)

# GitHub is a read-only public mirror. LastGit is source of truth for CR/CI/merge.
on:
  workflow_dispatch:

jobs:
  inert:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub mirror - no Actions. Use LastGit lastdb:///fold_db_website + code-node forge."
