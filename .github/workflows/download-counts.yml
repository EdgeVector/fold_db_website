name: download-counts.yml (mirror-inert)

# GitHub is a read-only public mirror. Scheduled Actions are disabled here.
on:
  workflow_dispatch:

jobs:
  inert:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub mirror - no scheduled download-count Actions. Use LastGit lastdb:///fold_db_website + code-node forge."
